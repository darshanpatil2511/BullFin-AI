-- Optional dev seed: populated after you sign up a test user through the app.
-- Replace the uuid below with a real auth.users id (run `select id from auth.users;`).
--
-- Example:
--   insert into public.portfolios (user_id, name, description)
--   values ('<your-auth-user-id>', 'Sample Portfolio', 'Seeded for local dev');

-- Intentionally empty — see comment above.
